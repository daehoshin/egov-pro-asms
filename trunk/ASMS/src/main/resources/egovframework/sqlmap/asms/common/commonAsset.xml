<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://www.ibatis.com/dtd/sql-map-2.dtd">

<sqlMap namespace="commonAsset">
	<insert id="AssetSequenceInsertForSearch" parameterClass="assetSequenceVO">
		INSERT /* common.EqPlMpSequenceInsertForSearch */
	    INTO SEQ_EQPLMP(SEQ_DT)
	    VALUES(#seq_dt#) 
	   <selectKey keyProperty="seq_id" resultClass="Integer">
	        SELECT LAST_INSERT_ID()
	   </selectKey>
	</insert>
	
	<select id="PlaceBaseInfoSearch" parameterClass="assetBaseInfoVO" resultClass="assetBaseInfoVO">
		SELECT /* common.PlaceBaseInfoSearch */ 
		     PLC_ID AS ASSET_PK
		   , PLC_NM AS ASSET_NM
		FROM TB_PLCDETAIL
		WHERE PLC_ID = #asset_pk#
	</select>

	<select id="ManPowerBaseInfoSearch" parameterClass="assetBaseInfoVO" resultClass="assetBaseInfoVO">
		SELECT /* common.ManPowerBaseInfoSearch */ 
		     MP_ID AS ASSET_PK
		   , MP_NM AS ASSET_NM
		FROM TB_MPDETAIL
		WHERE MP_ID = #asset_pk#
	</select>

	<select id="EquipmentBaseInfoSearch" parameterClass="assetBaseInfoVO" resultClass="assetBaseInfoVO">
		SELECT /* common.EquipmentBaseInfoSearch */ 
		     EQ_ID AS ASSET_PK
		   , EQ_NM AS ASSET_NM
		FROM TB_EQUIPMENTDETAIL
		WHERE EQ_ID = #asset_pk#
	</select>
	
</sqlMap>